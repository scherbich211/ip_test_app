// import {TLocationInfo} from '@Types/index';
// import {getPublicIP} from '@Utils/getPublicIP';
// import {useEffect, useState} from 'react';

// // Interface for Location Information

// // Custom hook to fetch location information
// const useGetLocationInfo = (ip?: string | null): {locationInfo: TLocationInfo | null; error: string | null} => {
  

//   return {locationInfo, error};
// };

// export default useGetLocationInfo;
